import { TerminalDashboard } from '@/components/terminal-dashboard';

export const dynamic = 'force-static';

export default function LpTerminal() {
  return <TerminalDashboard activePath="/lp" />;
}
